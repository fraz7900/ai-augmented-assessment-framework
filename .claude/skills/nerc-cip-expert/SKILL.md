---
name: nerc-cip-expert
description: Use when reading, writing, or reasoning about NERC CIP (Critical Infrastructure Protection) structure, standards, requirements, or applicability — including framework_mapping/nerc_cip.yaml, NERC-specific scoring or mapping logic, or any NERC CIP-specific documentation or demo content.
---

# NERC CIP Expert

Grounding knowledge for working with the NERC Critical Infrastructure Protection (CIP) standards in this project. Load this skill before editing `framework_mapping/nerc_cip.yaml`, NERC-specific mapping logic, or any content that claims to represent NERC CIP structure.

## Model structure

- NERC CIP is not one document — it is a **suite of independently numbered and versioned standards** (CIP-002 through CIP-014 currently mandatory; CIP-015 approved but "Subject to Future Enforcement," not yet in force as of ADR-0021's writing — check the live effective date before treating it as mandatory). Each standard is its own `Domain` in `framework_mapping/nerc_cip.yaml`, with `short_code` as the stable standard number (e.g. `"CIP-004"`) and the version-specific citation on `Domain.source_version`/`source_url` (e.g. `"CIP-004-7"`), not on `FrameworkDefinition` — unlike C2M2/NIST CSF 2.0, which each cite one single source document at the top level.
- Within a standard: **Standard → Requirement (R1, R2, ...) → Table of Parts** (columns: Part | Applicable Systems | Requirements | Measures), modeled as `Objective` (one per Requirement) → `Practice` (one per Part). `Objective.purpose` is the Requirement's own governing "shall" sentence; `Practice.text` is the Part's "Requirements" column (the substantive obligation); the "Measures" column is evidentiary detail and is deliberately not modeled as its own field.
- **Not every standard uses that table pattern — confirmed directly against source text for all 13, not assumed.** CIP-002 (impact categorization itself), CIP-003 (policy-based requirements), CIP-012 (Control Center communications), and CIP-014 (physical security of Transmission stations, not BES Cyber Systems) have **no "Applicable Systems" column at all** — their Parts are sub-numbered items in prose (e.g. CIP-002 R1's Parts 1.1-1.3), and `Practice.applicability` is genuinely empty for these, not a missed transcription. CIP-003/CIP-010/CIP-013 also each have at least one Requirement with **no sub-Parts at all** (e.g. CIP-003 R3, R4; CIP-010 R4; CIP-013 R2, R3) — the Requirement itself is the atomic practice, given a bare practice id with no decimal (e.g. `CIP-003-3`, not `CIP-003-3.1`). Check the real source text's structure before assuming the CIP-004/CIP-005-style table pattern applies.
- **`Practice.applicability` is NERC CIP's genuinely new structural concept** (added in ADR-0021): where a standard's "Applicable Systems" column exists, it scopes each Part to specific BES Cyber System impact tiers (High/Medium/Low) and sometimes to specific associated system types (EACMS, PACS) — not every Part applies to every impact tier. This has no analogue in C2M2 or NIST CSF 2.0, whose practices apply uniformly once a domain is in scope. Populate this from the real table text when a table exists (per the point above, several standards genuinely have none) — never leave it blank for a transcribed practice that does have a real Applicable Systems column.
- Each Requirement also carries a **Violation Risk Factor (Lower/Medium/High)** and a **Time Horizon** (e.g. "Operations Planning," "Same Day Operations") — real regulatory metadata, not currently modeled as schema fields (nothing in this project's scoring or UI consumes them yet). If a future sprint needs them, that is itself a schema-evolution decision per the framework-mapping skill, not something to bolt on ad hoc.
- **CIP-002 is foundational**: it defines the BES Cyber System impact-categorization process (High/Medium/Low) that every other standard's "Applicable Systems" column implicitly depends on. CIP-002 is fully transcribed (its own R1/R2, 5 Parts) like every other standard, but it is not itself modeled as a control the *other* standards' impact tiers are derived from computationally — that categorization logic remains real prose in CIP-002's own practices, not a cross-domain reference.

## Current transcription state (as of ADR-0022)

- **All 13 currently-mandatory standards are fully transcribed** into real Requirements/Parts — 141 total practices, matching `backend/scripts/generate_nerc_cip_yaml.py`'s own asserted `total_practices_in_source` exactly (verified at generation time and again at load time, mirroring the NIST CSF 2.0/C2M2 precedent). ADR-0021 started with CIP-004-7 only (19 Parts); ADR-0022 completed the remaining 12.
- Every domain has real, source-verified `purpose`, `source_version`, and `source_url`. `practices_populated` is `True` for all 13 — there are no remaining stubs in this framework.
- `scoring_model` is `"coverage"` (mirrors NIST CSF 2.0, ADR-0010) — NERC CIP is a compliance/audit standard, not a native maturity model. `Practice.mil` is always `None`.
- **Per-standard practice counts** (for a sanity check against a future re-transcription): CIP-002=5, CIP-003=5, CIP-004=19, CIP-005=12, CIP-006=14, CIP-007=20, CIP-008=12, CIP-009=10, CIP-010=12, CIP-011=4, CIP-012=5, CIP-013=4, CIP-014=19.

## Fetch method (confirmed, don't rediscover)

- `nerc.com` blocks `WebFetch` with a domain-wide WAF (403 on every path, including the plain root and Wayback Machine mirrors). A direct `curl` with a browser `User-Agent` header succeeds cleanly (HTTP 200, real `content-type: application/pdf`) — the same "WebFetch fails, curl succeeds" pattern ADR-0009 already documented for C2M2's own DOE source PDF. Use `curl` directly, not WebFetch, for any further NERC CIP source fetching.
- The canonical standards index is `https://www.nerc.com/standards/reliability-standards/cip` — it embeds a structured JSON page model (`pageModel.allStandards`, with `number`, `status`, `effectiveDate`, `url` fields) directly in the HTML. Parse that JSON, not scraped prose or search-result titles, to get exact current version numbers and effective dates.
- **Distinguish `"Mandatory Subject to Enforcement"` from `"Subject to Future Enforcement"`** in that JSON's `status` field before treating any standard/version as currently in force — the index lists both current and not-yet-effective future versions side by side (e.g. CIP-002-5.1a is mandatory now; CIP-002-7 and CIP-002-8 appear in the same feed with future effective dates).
- Individual standard PDFs redirect (`301`) from `/pa/Stand/Reliability%20Standards/{NUMBER}.pdf`-style legacy paths to `https://www.nerc.com/globalassets/standards/reliability-standards/cip/{number-lowercase}.pdf` — use the final redirected URL as `source_url`, not the legacy path.
- Extract PDF text with `pypdf` (the same tool ADR-0009 used for C2M2), and locate the `"3. Purpose:"` section for each standard's real purpose statement — do not paraphrase or reconstruct purpose text from memory/training data.

## Rules for this project

1. **Never hardcode NERC CIP structure in Python.** It belongs in `framework_mapping/nerc_cip.yaml`, generated by `backend/scripts/generate_nerc_cip_yaml.py`, per ADR-0002.
2. **Every domain's `source_version`/`source_url` must cite the specific version this data was transcribed from or verified current against** — NERC standards are revised more often than C2M2/NIST CSF 2.0, so a stale version citation here is a real defensibility risk, not a cosmetic one. Re-fetch and re-verify a standard's currency (via the JSON index's `status` field) before trusting an existing `source_version` is still the mandatory one.
3. **Do not fill in Requirements/Parts from memory or inference for any future framework revision.** Fetch and parse the real PDF first (curl + browser UA, per above), the same discipline ADR-0009/ADR-0018 already established for C2M2. If NERC issues a new version of any standard (e.g. CIP-003-10 supersedes CIP-003-9), treat updating it exactly like this project treated C2M2's original partial transcription — verify the new source text in full before touching the data.
4. **`Domain.practices_populated` is the single source of truth for whether a standard has real practices to score** — check it (or `total_practices_in_source`'s own assertion) rather than assuming full coverage implies nothing can still be wrong.
5. When connecting NERC CIP to the cross-framework mapping engine (`framework_mapping/cross_framework_equivalence.yaml`), remember that no NERC CIP entries exist yet — that is a separate, deliberate future human-review pass (see R-26/US-8.1b in the product docs), not automatic just because all 13 standards now have real practices to map against. Per the framework-mapping skill, equivalence entries must be individually human-reviewed with a rationale — never inferred by embedding similarity alone.
6. **Respect the real structural variance across standards** (see "Model structure" above) — do not force every standard into the CIP-004/CIP-005 Applicable-Systems-table shape. A standard with no such column, or a Requirement with no sub-Parts, is a real fact about that standard, not an incomplete transcription.

## Example usage

Adding NERC CIP cross-framework equivalence in a future sprint: load this skill to confirm which standards' practices exist to map against (all 13, post-ADR-0022), then follow the framework-mapping skill's two-stage compute-candidates-then-human-review process, the same one ADR-0019 used for C2M2↔NIST CSF 2.0 — never skip the human review step just because NERC CIP's data is now complete.
