Current sprint: Sprint 10 — Frontend (React) + Docker Compose deployment + full C2M2 transcription + cross-framework equivalence
Status: Complete, all four pieces live-verified. Frontend: `frontend/` went from a README-only placeholder to a working Vite/React/TypeScript app covering every persona's primary flow, closing NFR-4's UI-level requirement; verified live via a Playwright-driven walkthrough (zero console errors; two real bugs found and fixed). See ADR-0016. Deployment: `deployment/` went from a README-only placeholder to a real `docker-compose.yml` + Dockerfiles; live-verified end to end after fixing one real bug (`npm ci --legacy-peer-deps`) found on the first real build. Closes risk R-24. See ADR-0017. C2M2 transcription: the remaining 8 domains (285 of 356 practices) were transcribed from the real source PDF — C2M2 is now fully transcribed, all 10 domains, 356 of 356 practices. Closes risk R-14 and backlog item US-3.1a. See ADR-0018. Cross-framework equivalence (US-5.2/FR-14): 79 of 106 NIST CSF 2.0 subcategories now have a real, human-reviewed C2M2 equivalent (not an automatic similarity match — a hard constraint in `.claude/skills/framework-mapping/SKILL.md`), each with a rationale, surfaced as an "Also satisfies" note in the Evidence tab. Review caught a real false-positive pattern (C2M2's near-identical "Management Activities" boilerplate repeated across all 10 domains) that pure similarity ranking could not have distinguished. See ADR-0019.
Next: NERC CIP roadmap extension is the next largest remaining backlog item per prioritization.md. No blocking decisions outstanding.
Charter: PROJECT_CHARTER.md
