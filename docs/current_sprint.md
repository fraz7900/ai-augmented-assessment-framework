Current sprint: Sprint 10 — Frontend (React) + Docker Compose deployment stack + full C2M2 transcription
Status: Complete, with one disclosed gap. Frontend: `frontend/` went from a README-only placeholder to a working Vite/React/TypeScript app covering every persona's primary flow, closing NFR-4's UI-level requirement; verified live via a Playwright-driven walkthrough against the real running backend (zero console errors on the final pass; two real bugs found and fixed during that same verification). See ADR-0016. Deployment: `deployment/` went from a README-only placeholder to a real `docker-compose.yml` + Dockerfiles, one named volume for runtime state, Ollama gated behind a Compose profile per explicit direction rather than started by default. Statically verified (YAML validity, every env var cross-checked against the real `Settings` class, `pip install .` replicated in an isolated venv) but **not run end-to-end** — the authoring environment has no Docker installed and no passwordless `sudo` to install it; tracked as risk R-24. See ADR-0017. C2M2 transcription: the remaining 8 domains (285 of 356 practices) were transcribed from the real source PDF, following the exact process ADR-0009 established — C2M2 is now fully transcribed, all 10 domains, 356 of 356 practices matching the source's own stated total. Closes risk R-14 and backlog item US-3.1a. Two tests that hardcoded the old partial-coverage state were updated; full 181-test suite re-verified passing. See ADR-0018.
Next: run the Docker Compose stack live and close R-24 (needs a machine with Docker available); otherwise NERC CIP roadmap extension or cross-framework equivalence mapping (US-5.2, now buildable against full C2M2+NIST CSF 2.0 data), per prioritization.md.
Charter: PROJECT_CHARTER.md
