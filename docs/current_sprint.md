Current sprint: Sprint 10 — Frontend (React) + Docker Compose deployment stack + full C2M2 transcription
Status: Complete, all three pieces live-verified. Frontend: `frontend/` went from a README-only placeholder to a working Vite/React/TypeScript app covering every persona's primary flow, closing NFR-4's UI-level requirement; verified live via a Playwright-driven walkthrough against the real running backend (zero console errors on the final pass; two real bugs found and fixed during that same verification). See ADR-0016. Deployment: `deployment/` went from a README-only placeholder to a real `docker-compose.yml` + Dockerfiles, one named volume for runtime state, Ollama gated behind a Compose profile rather than started by default. Initially only statically verified (no Docker in the authoring environment); once Docker Desktop was installed and its WSL2 integration enabled, a full live run found and fixed one real bug (`npm ci` needed `--legacy-peer-deps`) and then passed end to end — build, up (backend/frontend only, ollama correctly excluded), a full persona walkthrough (zero console errors, 349 AI mapping candidates proposed reflecting the full C2M2 transcription), restart-persistence, and the `--profile ollama` opt-in path all confirmed. Closes risk R-24. See ADR-0017. C2M2 transcription: the remaining 8 domains (285 of 356 practices) were transcribed from the real source PDF, following the exact process ADR-0009 established — C2M2 is now fully transcribed, all 10 domains, 356 of 356 practices matching the source's own stated total. Closes risk R-14 and backlog item US-3.1a. See ADR-0018.
Next: NERC CIP roadmap extension, or cross-framework equivalence mapping (US-5.2, now buildable against full C2M2+NIST CSF 2.0 data), per prioritization.md. No blocking decisions outstanding.
Charter: PROJECT_CHARTER.md
