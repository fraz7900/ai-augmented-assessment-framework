# deployment/

Dockerfiles and docker-compose configuration for running the platform locally as a full stack (backend, frontend, Ollama, vector store). Local containerized deployment is in scope for the MVP; cloud deployment is explicitly deferred (see `PROJECT_CHARTER.md` Section 12, Future Roadmap) and belongs in `infrastructure/` if and when it happens.
