# data/raw/

Landing zone for documents as originally uploaded, before any parsing or chunking. Contents are gitignored (see `.gitignore` and `docs/adr/ADR-0002-data-as-code-separation.md`) — only this README is tracked.

Only public framework documentation or synthetic sample evidence should ever be placed here during development. See `data/sample_evidence/README.md` for the actual tracked example set used in demos.
