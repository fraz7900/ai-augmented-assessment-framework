# infrastructure/

Infrastructure-as-code for a future cloud/multi-tenant deployment (Terraform or similar), explicitly out of scope for the MVP (see `PROJECT_CHARTER.md` Section 12). Kept as an empty, documented placeholder now rather than added later, so the roadmap in the charter has a concrete home to point to and the repository structure doesn't need to change shape if the project extends beyond a portfolio artifact.
